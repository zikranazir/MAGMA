llm:
  provider: openai
  model: gpt-4o-mini
  temperature: 0.7
  max_tokens: 4096

memory:
  enabled: true
  backend: sqlite
  sqlite_path: ./checkpoints.db

server:
  host: 0.0.0.0
  port: 8000

logging:
  level: INFO
  format: json
